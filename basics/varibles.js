const id=12 //once declare in const value can't change again
let name="yash" // varible declare in let have block scope and can't redeclare in same scope and block scope
var city="bengaluru"//variable declare in var can be redclared and have globle scope 
state="maharastra"
let country;// if value dosen't declare then the output will be undefined

console.log(id)
console.table([name,city,state.country]) //output will print in table format